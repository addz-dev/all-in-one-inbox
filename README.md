# all-in-one-inbox
The aioi (all in one inbox) issue board and bug reports

## Issue reporting

This repository is configured for structured bug reporting through GitHub Issue Forms.

Available issue types:
- **Bug report** (applies `bug`)
- **Client-reported bug** (applies `bug` and `client reported`)
